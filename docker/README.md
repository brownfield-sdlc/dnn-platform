# DNN Platform SQL Server Docker

Minimal Docker environment for static schema analysis.

## Quick Start
```bash
cd docker
docker-compose -f sql-server/docker-compose.yml up -d sql-server
```

See root DNN_DOCKER_SETUP.md for queries.
